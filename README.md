# Nur E-commerce - ইউজার অ্যাপ

"Nur" একটি আধুনিক, দ্রুত এবং ব্যবহারকারী-বান্ধব ই-কমার্স ওয়েবসাইট। এটি ভ্যানিলা জাভাস্ক্রিপ্ট এবং Firebase ব্যবহার করে একটি সিঙ্গেল পেজ অ্যাপ্লিকেশন (SPA) হিসেবে তৈরি।

## ✨ বৈশিষ্ট্য (Features)

-   **আধুনিক UI/UX:** পরিচ্ছন্ন এবং মোবাইল-ফার্স্ট ডিজাইন।
-   **সিঙ্গেল পেজ অ্যাপ্লিকেশন (SPA):** পেজ রিলোড ছাড়াই দ্রুত নেভিগেশন।
-   **মডুলার কোডবেস:** রক্ষণাবেক্ষণযোগ্য জাভাস্ক্রিপ্ট মডিউল।
-   **ডেমো মোড:** Firebase কানেক্ট না থাকলেও স্যাম্পল ডেটা দিয়ে ওয়েবসাইটটি চলবে।

## 🚀 কিভাবে চালাবেন (Setup Instructions)

1.  **প্রজেক্ট ক্লোন করুন:**
    ```bash
    git clone <your-repo-link>
    cd nur-ecommerce-user-app
    ```

2.  **(ঐচ্ছিক) Firebase সংযোগ করুন:**
    -   `js/services/config.example.js` ফাইলটিকে `config.js` নামে রিনেম করুন।
    -   Firebase থেকে আপনার ওয়েব অ্যাপের কনফিগারেশন কপি করে `config.js` ফাইলে পেস্ট করুন।
    -   Firestore-এ `products` নামে একটি কালেকশন তৈরি করে কিছু ডেটা যোগ করুন।

3.  **লাইভ সার্ভার দিয়ে চালান:**
    -   VS Code-এর **Live Server** এক্সটেনশন ব্যবহার করে `index.html` ফাইলটি ওপেন করুন।

## 🏛️ আর্কিটেকচার

এই প্রজেক্টটি একটি মডুলার SPA আর্কিটেকচার অনুসরণ করে:
-   `app.js`: মূল কন্ট্রোলার এবং রাউটার।
-   `store.js`: কেন্দ্রীয় ডেটা ম্যানেজমেন্ট।
-   `ui.js`: শুধুমাত্র HTML রেন্ডারিং-এর কাজ করে।
-   `api.js`: ডেটাবেসের সাথে যোগাযোগ করে।
-   `auth.js`: ব্যবহারকারীর লগইন/সাইন-আপ পরিচালনা করে।

## 🔮 ভবিষ্যৎ পরিকল্পনা

এই ইউজার অ্যাপটি সম্পূর্ণভাবে তৈরি হওয়ার পর, একটি আলাদা ফোল্ডারে **অ্যাডমিন প্যানেল** তৈরি করা হবে। অ্যাডমিন প্যানেলটি একই Firebase প্রজেক্ট ব্যবহার করে প্রোডাক্ট, অর্ডার এবং ব্যানার ম্যানেজ করতে পারবে।
